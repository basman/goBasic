package main

func main() {
	/*
			TODO Simple Game: Guess the Number
		        Create a program that generates a random number between 1 and 100.
		        Ask the user to guess the number.
		        Provide hints such as "Too high" or "Too low" after each guess.
		        Continue until the user guesses the correct number.
	*/
}
