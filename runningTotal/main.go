package main

func main() {
	// in a loop query the user for a number and then print the running total

	// allow user to undo the last addition using 'u' as input

	// allow user to quit using 'q'

	// terminate program the same way when user closes the stdin file handle (using Ctrl-D) as if user had entered 'q'
}
